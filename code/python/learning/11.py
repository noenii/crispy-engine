#functions are first class objects, which means they can be treated like variables
#you can assign functions to variables
#pass them as arguments to other functions
#return them from functions
#store them in data structures such as lists or dictionaries

#map() is used to apply a function to every item in an iterable
#or smth like that (list, tuple, set, dicts...)

#reduce() from functools to apply functions to reduce iterables

#filter() is a function to filter iterables with another function

#nested functions r cool